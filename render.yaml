services:
  - type: web
    name: amip-backend
    runtime: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: ALPHA_VANTAGE_KEY
        sync: false
